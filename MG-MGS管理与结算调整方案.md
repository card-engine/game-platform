# MG / MGS 管理与结算调整方案

状态：方案已确认并实施；IP 修复已先行上线。本轮按以下最终规则验证和交付，线上数据清理使用独立命令。

## 一、范围和已经确认的事实

| 位置 | 当前情况 | 本次目标 |
| --- | --- | --- |
| `/game/users` | MGS 已传玩家 IP，MG 却记录了内部请求的 `127.0.0.1` | 接收已验签的玩家 IPv4/IPv6；不影响商户来源白名单 |
| `/game/settings` | 统计时区、统计币种可修改，会触发历史统计重建 | 首次安装确定，数据库持久化，后台和保存接口只读 |
| `/game/bills`、`/mgs/bills` | 零派奖也生成资金流水，现有流水还承担幂等及回调重试 | 资金流水只记实际资金变化；结单事件独立处理 |
| `/mgs/overview` | 币种选择器占宽 | 约 112px，靠右，不挤压标题 |
| `/mgs/users` | 能筛选启用/停用，没有状态操作入口 | 增加禁用、启用；同步处理登录、进游、下注与历史结算边界 |
| `/mgs/bets` | 显示平台服务费，实际是逐笔预估 | 改为“预估平台服务费”，与正式月账单区分 |
| `/mgs/settlements` | 只有生成；没有确认、付款操作；金额沿用逐笔预估之和 | 正式月账单作为费用依据，补齐核对、确认、结清和权限 |
| `/mgs/recharges`、`/mgs/transfers` | 状态直接显示文本 | 用统一的 ElTag 颜色展示状态 |

已确认服务器公网 IP 是正常来源，保留该类记录，不调整 Nginx、代理头或后台登录 IP。

代码核对发现：

- MG 的 `MonthlyBillingService::generateGgr()` 已按已结单时间、商户账期时区、币种和注单费率快照汇总正负 GGR。
- MGS 的 `MgsCallbackService::updateBet()` 按每笔正 GGR 算预估费用；`MgsStatsService` 直接相加，`MgsSettlementService` 又拿该合计生成结算单。因此正式月结不能继续以 MGS 的逐笔预估费用合计为依据。
- MGS 回调现在在入口统一拒绝停用用户，连历史派奖、取消也会拒绝。仅补一个禁用按钮是不完整的。
- 两套资金流水同时作为回调事件记录。零派奖不能直接 early return，否则会丢掉下游结单、响应余额、幂等或异常重试。

## 二、玩家 IP：先行的小范围修复

- MGS 沿用现有 `ip` 参数；该参数参与商户签名。
- MG 进游接口使用校验器的 `filter:validate_ip`，同时支持 IPv4、IPv6。
- 未传或传空值：已有用户不覆盖 `last_ip`，新用户留空。
- 后台直接试玩仍使用操作者请求 IP。
- 商户 IP 白名单仍校验调用服务器来源；不改成校验玩家 IP。
- 不批量用 MGS 最新登录 IP 覆盖 MG 历史进游 IP；两者对应时刻可能不同。修复后下一次成功进游自然更新。

验证覆盖：签名、IPv4、IPv6、未传 IP、非法 IP、篡改 IP 后签名失败、来源白名单独立、后台试玩。测试使用隔离数据库及本机模拟上游，不向真实供应商创建测试会话。

## 三、平台统计口径：首次安装确定，后续只读

采用“.env 提供初始值 → 首次写入 mg_configs → 运行时读取数据库永久缓存”的方式，不把日常读取改成直接读 .env。

建议 `.env.example` 增加：

```dotenv
# 仅首次安装时初始化 MG 平台统计口径。
# 安装前确定；写入数据库后只读，后续修改 .env 或执行升级不会覆盖已有值。
INITIAL_PLATFORM_TIMEZONE=Asia/Shanghai
INITIAL_PLATFORM_CURRENCY_CODE=USD
```

当前线上值正是 `Asia/Shanghai` 和 `USD`，接入新规则时保留已有值。

实现边界：

1. `database/system.php` 定义默认值及说明，复用 `SystemDataService` 已有“只在新增时写 value”的逻辑；首次写入验证 IANA 时区和币种格式。
2. 后台两项改成只读文本，标注“首次安装确定”。读取数据库生效值，而不是显示 .env 当前文本。
3. `SettingsLogic::save()` 只允许保存 `exchange_rate_display_codes`，拒绝安装配置及其他未开放字段；超级管理员也不能通过该接口修改。
4. 保留常用汇率币种选择及保存按钮；移除“修改这两项就重建历史数据”的交互与自动派发。日常统计及独立维护重算能力不受影响。
5. 不新增通用只读配置框架、数据库触发器或 readonly 字段，也不修改 SaiAdmin 产品代码。
6. 这里只固定 MG 平台统计口径，不混用 MGS 的统计时区、玩家钱包币种或商户合同账期时区。

## 四、零派奖：资金流水和交易事件分开

### 4.1 数据职责

建议让现有 prepare / complete 流程改为围绕“交易事件”工作，而不是把每次通知都先创建成资金流水。

| 数据 | 职责 |
| --- | --- |
| MG / MGS 各自的交易事件 | 交易号去重、请求摘要、关联注单、处理状态、回调响应、结果未知重试；零金额也可有事件 |
| `mg_bills_*` / `mgs_bills_*` | 已确认的实际资金变化；零金额不生成 BL / ML 流水 |
| `mg_bets_*` / `mgs_bets_*` | 对局金额汇总、结单状态和时间、操作轨迹 |

两套业务分别存储事件，不共享业务表。建议增加 `mg_trade_events_template`、`mgs_trade_events_template` 并沿用月表管理；schema 基线、月表预建和 db:upgrade 同步支持事件模板，不在请求中新增 DDL。事件承担可变的幂等、重试和处理状态；正常资金流水保留当时成功请求及响应的只读快照，后续重试只查事件，不查询旧流水兜底。

事件最少保留：稳定交易标识、请求摘要、用户/币种/动作、注单引用、有效金额、待处理/成功/失败/结果未知状态、请求与结果、UTC 处理时间。数据库唯一索引保证同一交易只处理一次。复用现有交易服务，不引入新的通用交易框架。

### 4.2 处理规则

```text
验签、校验请求 → 查交易事件幂等
                   ↓
            通知/执行商户钱包协议
                   ↓
       本地事务确认事件并更新注单
                   ↓
   有实际金额：记录资金流水；零金额：不记资金流水
```

- `win_amount = 0` 且 `is_end = 1`：正常同步结单，两边注单结束；不更新玩家钱包余额和 version，不生成零金额资金流水。
- `win_amount = 0` 且 `is_end = 0`：只处理事件，不提前结单。
- 零金额下注或免费局同样不因“金额合计为零”被误判为撤销；结束状态由结束/撤销事件决定。
- 取消请求中的 `cancel_amount = 0` 仍按既有协议解释为“全额取消”，先计算实际退款额；实际退款不为零就正常记流水。
- 是否为零使用原始十进制金额和 BCMath 判断，不能按 UI 的 `0.00` 判断。
- MG 必须继续把结单通知传到下游；收到业务成功才确认自己的处理完成。回调超时保留未知事件并用相同交易号重试。
- MGS 原子保存事件结果和注单变化；重复请求返回已保存结果，相同交易号不同参数拒绝。
- 自动结单沿用相同事件流程，不能再制造零派奖资金流水。
- 结单仍完成预留额度释放、结算时间记录、统计刷新及延迟任务取消，不是单独执行一次 `status=2`。
- 资金流水笔数只计实际金额变动；注单详情把零结束动作显示为“结单”，不生成虚构资金流水号。

### 4.3 现有零记录与切换

按新系统设计，不添加旧格式双读、旧算法兼容或历史分支。

- 指定的 `BL260919052557e3c56c3c6b51` 及对应 MGS 零派奖流水将从资金流水表物理删除，不是只在列表隐藏。
- 默认不需要 truncate 整个库。发布时通过独立数据脚本移交必要的协议处理凭据，删除零资金流水，整理注单动作中的流水引用和流水笔数，再重算受影响统计。
- 数据处理与结构升级分开，先列出受影响数量。处理后核对零派奖资金流水数为 0、钱包余额不变、注单仍正确结单。
- 若选择全部舍弃游戏测试交易，可成组重置对应事件、注单、流水和统计；不能只清一张表留下关联和待重试任务。充值到账记录、钱包和配置不因为本需求被顺带清空。

## 五、预估费用与正式月结统一

### 5.1 正式费用口径

MG 正式 GGR 月结已经实行：同一商户、同一币种、同一结算月内，正负 GGR 抵消。按已结束注单的 `settled_time` 归属账期，使用商户账期时区；GC 不收费，不同币种不直接相加。

同一费率时：

```text
月服务费 = max(月内净 GGR, 0) × 费率
```

例：两笔 GGR 分别为 +100、-60，费率均为 3%。逐笔正 GGR 预估合计为 3.00，正式月费应为 1.20。

月内有不同费率时沿用 MG 现有规则：

```text
净 GGR = Σ GGR
加权费用 = Σ (GGR × 各注单费率快照)
正式费用 = 净 GGR > 0 时取 max(加权费用, 0)，否则为 0
```

负月份不自动跨月抵扣；不把月末当前费率回填到全部历史注单。

### 5.2 页面和计算调整

- `/mgs/bets` 的平台服务费改名“预估平台服务费”，说明“单笔估算，最终以月结账单为准”。相关概览、日报中的同源金额也统一标为预估。
- 不全局替换 `platformFee` 文案，避免把结算单中的正式服务费也改成预估。
- 停止用 MGS 月统计里的逐笔预估费用合计直接生成正式结算单。
- 正式费用以 MG 为自营商户生成的月账单为唯一依据。MGS 通过商户签名的只读账单接口获取自己的账期、币种、费用、费率明细和状态；不跨模块直接查询 MG 业务表。
- 月内费率变化时显示费率明细，不把预估合计除以净 GGR 伪装成一个合同费率。
- 只允许生成已结束月份的正式结算；当前月只能预览。

## 六、自营用户禁用与资金链路

- `/mgs/users` 增加“禁用 / 启用”操作，复用现有 `status=0/1`。禁用需填写原因并二次确认，记录管理员操作日志。
- 使用独立按钮权限 `app:mgs:user:update`；前端 v-permission 与后端 Permission 一致，内置角色同步授权。
- 默认保护用于后台试玩的系统玩家；普通用户可禁用和恢复。
- 停用后允许登录、游戏列表、资料、余额与充值记录查询。只在新进游、新建充值、修改资料时要求启用，复用 `MgsAuthService::browserUser()` 的可选状态校验，不增加查询或鉴权中间件。
- 按用户最终确认：已进入游戏的会话继续处理下注、派奖、取消和余额回调；MGS 停用不会连带禁用 MG 玩家，下次从自营平台进游戏才被拒绝。没有强退或立刻停赌逻辑。
- 已成功交易的重复通知先返回幂等结果，不因后来禁用用户而把已成功的扣款改成失败。新操作再校验状态。
- 既有签名游戏会话继续正常建立新局和结算，取消仍必须关联原交易；不把停用玩家等同于删除玩家。
- 协议需要的余额查询仍可服务已授权的结算链路，浏览器只读账户查询允许访问。
- 已付款的充值不丢弃：继续沿用停用用户转人工核验的规则，不自动重放入账。
- 解除禁用恢复原账号，不修改余额、不新建账号、不通过清空浏览器凭证实现禁用。

## 七、自营结算的完整操作流程

### 7.1 谁操作

| 角色 | 操作 |
| --- | --- |
| 自营运营 | 生成待确认单、查看对账明细、补充核对备注 |
| 获授财务权限的游戏超管 | 确认结算、登记付款/结清 |
| 普通只读账号 | 查看，无确认或付款权限 |

新增精确的 `app:mgs:settlement:confirm`、`app:mgs:settlement:pay` 权限。游戏超管角色补充相应 MGS 结算菜单和权限，自营运营不默认获得资金确认权限。

### 7.2 状态和按钮

```text
已结束月份 → 生成/刷新待确认单 → 财务核对并确认 → 登记付款、核销 → 已结清
```

- 待确认：可重新生成；同一月份、币种保持一张单。
- 确认：展示并核对来源 MG 账单号、月份、账期时区、币种、净 GGR、费率明细、正式服务费和 MGS 净额；保存确认人、确认时间、来源快照。
- 已确认待结清：金额不可直接编辑。支付前再次核对来源账单，发生变化时退回核对，不能按旧快照支付。
- 登记付款：填写实际结算时间、凭证/业务参考号和备注。操作只登记核销事实，不执行银行或链上转账。
- 正式账单生成和读取通过商户签名的 `/open_api/monthly-bills/generate`、`/open_api/monthly-bills` 完成。登记结清仅由管理员后台发起；在当前同库部署内直接复用 MG `MerchantLogic::billStatus()` 的授权、锁和核销事务，MGS 确认状态与 MG 核销一起提交。没有商户可自行标记付款的公开接口。来源账单号/商户/快照不符即拒绝，两边不重复收费。
- 零费用结算确认后显示“已结清·无需付款”，不制造零金额付款流水。
- 已支付/已结清：不能重复核销或直接改回未支付。迟到回调造成差额，通过有原单引用的调整流程处理，不静默覆盖。

补齐结算单详情与操作栏；确认人、时间、支付凭证及来源单号均可追溯。用户钱包不参与部门服务费核销。

## 八、其余页面调整

### 8.1 概览币种选择器

`/mgs/overview` 选择器使用约 112px 的业务样式宽度，不随整行拉伸，右对齐；手机标题可换行，选择器仍保持紧凑。仅修改业务页面。

### 8.2 状态颜色

复用 ElTag，跨两个页面保持同一含义、同一颜色：

| 页面 | 状态 | 颜色 |
| --- | --- | --- |
| 充值记录 | pending 待付款 | 蓝色 |
| 充值记录 | paid 已到账 | 绿色 |
| 充值记录 | review 待核验 | 橙色 |
| 充值记录 | expired 已过期、closed 已关闭 | 灰色 |
| 链上收款 | pending 待处理 | 蓝色 |
| 链上收款 | credited 已入账 | 绿色 |
| 链上收款 | review 待核验 | 橙色 |
| 链上收款 | ignored 已忽略 | 灰色 |

不靠颜色代替文字，不把进入队列/识别到收款显示为已到账。

## 九、实施顺序和验收

1. IP 修复先单独提交、上线，核对实际调用链。
2. 安装配置只读、概览选择器、状态标签等边界明确的调整。
3. 交易事件与资金流水分离，MG / MGS 同步改零派奖、自动结单、重试；独立执行零流水清理。
4. 用户禁用入口与历史结算放行，和第 3 步共用交易边界验证。
5. 预估费用文案、正式月账单来源和结算确认/结清流程同步落地。

核心验收：

- IP：IPv4/IPv6、未传值、来源白名单、签名篡改。
- 固定配置：空库初始化、非法初始值拒绝、重复升级不覆盖、保存接口拒绝改只读项。
- 零金额：无 BL/ML 零资金流水、两边注单结单、钱包余额/version 不变、重复请求、参数冲突、回调未知重试、跨月及自动结单、零参数全额退款。
- 禁用：旧 token 可登录读取、新进游被拒、已进入游戏可继续下注、历史正派奖/零结单/退款完成、并发边界、重新启用。
- 月结：正负抵消、多档费率、多币种、GC 排除、已结束月份、跨月结单、确认权限、重复核销、来源变更和零费用结清。
- 前端：桌面/窄屏布局、状态文字与颜色、权限按钮、金额保持最少两位且不损失有效小数。
- 交付：PHP 检查、相关回归、两端必要构建、schema/内置数据升级检查、线上版本和业务结果分别核对。


## 发布与数据核对

- `php webman trade:events`：只读预览，列出每张旧流水表的移交与删除数量。
- `php webman trade:events --prepare-only`：安装/发布时预建当前、前两月、后两月及已有历史月份的事件表。日常月表任务也预建事件表；事件请求不会自动 DDL。
- 停止 Webman 交易服务并备份后执行 `php webman trade:events --apply`：复制交易凭据到事件表，物理移除零金额和非成功资金流水，调整注单操作轨迹与资金流水计数。充值等非游戏流水不参与。
- 清理后重算受影响 MG 日统计；逐钱包核对余额和 version、核对 GGR 与结单状态不变、核对指定零流水不存在。
- 旧版尚未确认的 MGS 结算在生成时被正式 MG 账单覆盖；已确认单不被后台重生成覆盖。未结束月份的旧测试结算须单独处理，不按正式账单确认。

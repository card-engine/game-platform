# GGR 月结

- 每笔净 GGR = 投注 − 投注回滚 − 派奖 + 派奖回滚，保留正负数。
- 以首次结单 UTC 时间换算商户时区确定自然月。跨月结单归入结束月份，补发不改变首次结单时间。
- 按商户、币种汇总。GC、未结单、不参与结算和阶梯月费注单不计入 GGR 费用。
- 月度净 GGR ≤ 0 时费用为 0；否则费用为 `max(Σ(注单净 GGR × 注单费率快照), 0)`。统一费率时等价于 `max(月度净 GGR, 0) × 费率`。
- 注单中预留额度，结单时释放；月结生成应付并扣减可用额度。旧版逐笔已收费用抵扣，不重复收费。
- 每小时定时检查已结束月份，自动补齐遗漏账单。账单、额度、月度用量和流水在同一事务提交，以币种额度行锁及账单唯一键保证重复执行不重复计费。
- 未支付账单收到迟到派奖或回滚时，重算并记录差额流水。已支付或减免账单发生变化时任务报错，需管理员对账调整，不能静默改写。
- 后台商户计费设置展示各币种 GGR 账单；标记支付会核销应付，减免同时返还占用额度。

月结账单是最终费用依据；单笔 `merchant_fee` 保留旧版已收金额，仅供升级抵扣，不能用其合计代替新版月结费用。自营平台 MGS 的统计及结算是独立账套，需单独核对其费用口径。

验证：`php tests/GgrMonthlyBillingSmoke.php`、`php tests/MonthlyBillingSmoke.php`、`php tests/TradeServiceSmoke.php`。数据库升级使用 `php webman db:upgrade --dry-run` 后执行 `php webman db:upgrade`。
